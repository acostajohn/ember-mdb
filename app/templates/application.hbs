<div class="container">
  <header>
    <div class="page-header">
      <h1 id="title">Ember Movies Database <small>by John Acosta</small></h1>
    </div>
  </header>

  {{#if session.isAuthenticated}}
    <nav>
      <ul class="nav nav-pills">
        <li class="active" data-js="main-page-link">{{#link-to "index"}}Main page{{/link-to}}</li>
        <li><a {{ action 'invalidateSession' }} data-js="logout-link">Log out</a></li>
      </ul> 
    </nav>
  {{/if}}

  {{outlet}}
</div>
